# App name
spring.application.name=userManagement

# Server configuration
server.port=9006

# Data base configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/userManagement_db
spring.datasource.username=postgres
spring.datasource.password=Lucas1998*

# JPA / Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
## application.properties (Développement):  À utiliser uniquement en local/test
app.jwt.secret=jwt.secret = Aq3t6w9z$C&F)J@NcRfUjXn2r5u8x/A?

## application.properties (Production): Remplacé par une variable d'environnement
#app.jwt.secret=${JWT_SECRET_KEY}
app.jwt.expiration-ms=3600000

# Logging configuration
logging.level.org.springframework.data=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.com.api.userManagement=DEBUG

logging.level.com.api.userManagement.security=DEBUG
logging.level.org.springframework.web=DEBUG


